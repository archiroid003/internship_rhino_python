# coding: utf-8
import rhinoscriptsyntax as rs

print(circle_packs)
circle_num = len(circle_packs)

for i in range(circle_num):
    my_circle = circle_packs[i]
    
    for j in ragen(circle_num):
        you_circle = circle_packs[j]
        
        if my_circle == you_circle:
            continue
        
        #-TODO この続きを書く
